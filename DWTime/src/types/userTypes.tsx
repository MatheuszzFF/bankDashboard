export type TUserSettings = {
    email: string,
    name: string,
    userName: string,
    birth: string,
    address: string,
    city: string,
    postalCode: number,
    country: string,
    theme: string;
    notifications: boolean;
    locale: string;
    language: string;
  }
